import 'package:flutter/material.dart';
import 'package:untitled15/model/category.dart';

const data=const[
categ(color: Colors.red),
  categ(color: Colors.lightBlueAccent),
  categ(color: Colors.limeAccent),
  categ(color: Colors.black),
  categ(color: Colors.blue),
  categ(color: Colors.deepOrange),
  categ(color: Colors.grey),
  categ(color: Colors.red),
  categ(color: Colors.lightBlueAccent),
  categ(color: Colors.limeAccent),
  categ(color: Colors.black),
  categ(color: Colors.blue),
  categ(color: Colors.deepOrange),
  categ(color: Colors.grey)

];